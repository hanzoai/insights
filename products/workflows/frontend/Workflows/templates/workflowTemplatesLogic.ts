import { MakeLogicType, actions, afterMount, kea, path, reducers, selectors } from 'kea'
import { loaders } from 'kea-loaders'
import { actionToUrl, router, urlToAction } from 'kea-router'

import api from 'lib/api'
import { createFuse, Fuse } from 'lib/utils/fuseSearch'

import type { InsightsFlowTemplate } from '../insightsflows/types'

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface workflowTemplatesLogicValues {
    availableTags: string[]
    filteredTemplates: InsightsFlowTemplate[]
    tagFilter: string | null
    templateFilter: string
    workflowTemplateFuse: Fuse<InsightsFlowTemplate>
    workflowTemplates: InsightsFlowTemplate[]
    workflowTemplatesLoading: boolean
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface workflowTemplatesLogicActions {
    deleteHogflowTemplate: (template: InsightsFlowTemplate) => {
        template: InsightsFlowTemplate
    }
    deleteHogflowTemplateFailure: (
        error: string,
        errorObject?: any
    ) => {
        error: string
        errorObject?: any
    }
    deleteHogflowTemplateSuccess: (
        workflowTemplates: InsightsFlowTemplate[],
        payload?: {
            template: InsightsFlowTemplate
        }
    ) => {
        workflowTemplates: InsightsFlowTemplate[]
        payload?: {
            template: InsightsFlowTemplate
        }
    }
    loadWorkflowTemplates: () => any
    loadWorkflowTemplatesFailure: (
        error: string,
        errorObject?: any
    ) => {
        error: string
        errorObject?: any
    }
    loadWorkflowTemplatesSuccess: (
        workflowTemplates: InsightsFlowTemplate[],
        payload?: any
    ) => {
        workflowTemplates: InsightsFlowTemplate[]
        payload?: any
    }
    setTagFilter: (tag: string | null) => {
        tag: string | null
    }
    setTemplateFilter: (search: string) => {
        search: string
    }
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface workflowTemplatesLogicMeta {
    __keaTypeGenInternalSelectorTypes: {
        workflowTemplateFuse: (workflowTemplates: InsightsFlowTemplate[]) => Fuse<InsightsFlowTemplate>
        filteredTemplates: (
            workflowTemplates: InsightsFlowTemplate[],
            templateFilter: string,
            tagFilter: string | null,
            workflowTemplateFuse: Fuse<InsightsFlowTemplate>
        ) => InsightsFlowTemplate[]
        availableTags: (workflowTemplates: InsightsFlowTemplate[]) => string[]
    }
}

export type workflowTemplatesLogicType = MakeLogicType<
    workflowTemplatesLogicValues,
    workflowTemplatesLogicActions,
    Record<string, any>,
    workflowTemplatesLogicMeta
>

export const workflowTemplatesLogic = kea<workflowTemplatesLogicType>([
    path(['products', 'workflows', 'frontend', 'Workflows', 'workflowTemplatesLogic']),
    actions({
        setTemplateFilter: (search: string) => ({ search }),
        setTagFilter: (tag: string | null) => ({ tag }),
        deleteHogflowTemplate: (template: InsightsFlowTemplate) => ({ template }),
    }),
    reducers({
        templateFilter: [
            '' as string,
            {
                setTemplateFilter: (_, { search }) => search,
            },
        ],
        tagFilter: [
            null as string | null,
            {
                setTagFilter: (_, { tag }) => tag,
            },
        ],
    }),
    loaders(({ values }) => ({
        workflowTemplates: [
            [] as InsightsFlowTemplate[],
            {
                loadWorkflowTemplates: async (): Promise<InsightsFlowTemplate[]> => {
                    const response = await api.hogFlowTemplates.getInsightsFlowTemplates()
                    return response.results as InsightsFlowTemplate[]
                },
                deleteHogflowTemplate: async ({ template }) => {
                    await api.hogFlowTemplates.deleteInsightsFlowTemplate(template.id)
                    return values.workflowTemplates.filter((t) => t.id !== template.id)
                },
            },
        ],
    })),
    selectors({
        workflowTemplateFuse: [
            (s) => [s.workflowTemplates],
            (workflowTemplates: InsightsFlowTemplate[]): Fuse<InsightsFlowTemplate> => {
                return createFuse(workflowTemplates || [], {
                    keys: [{ name: 'name', weight: 2 }, 'description'],
                    threshold: 0.3,
                    ignoreLocation: true,
                })
            },
        ],
        filteredTemplates: [
            (s) => [s.workflowTemplates, s.templateFilter, s.tagFilter, s.workflowTemplateFuse],
            (
                workflowTemplates: InsightsFlowTemplate[],
                templateFilter: string,
                tagFilter: string | null,
                workflowTemplateFuse: Fuse<InsightsFlowTemplate>
            ): InsightsFlowTemplate[] => {
                let filtered = workflowTemplates

                // Filter by tag
                if (tagFilter) {
                    filtered = filtered.filter((template) => template.tags.includes(tagFilter))
                }

                // Filter by search term using Fuse
                if (templateFilter) {
                    const searchResults = workflowTemplateFuse.search(templateFilter)
                    filtered = searchResults.map((result: { item: InsightsFlowTemplate }) => result.item)
                    // Apply tag filter to search results if active
                    if (tagFilter) {
                        filtered = filtered.filter((template) => template.tags.includes(tagFilter))
                    }
                }

                return filtered
            },
        ],
        availableTags: [
            (s) => [s.workflowTemplates],
            (workflowTemplates: InsightsFlowTemplate[]): string[] => {
                const tagSet = new Set<string>()
                workflowTemplates.forEach((template) => {
                    template.tags.forEach((tag) => tagSet.add(tag))
                })
                return Array.from(tagSet).sort()
            },
        ],
    }),
    urlToAction(({ actions }) => ({
        '/workflows': (_, searchParams) => {
            if (searchParams.templateFilter) {
                actions.setTemplateFilter(searchParams.templateFilter)
            }
            if (searchParams.tagFilter) {
                actions.setTagFilter(searchParams.tagFilter)
            }
        },
    })),
    actionToUrl(({ values }) => ({
        setTemplateFilter: () => {
            const searchParams = { ...router.values.searchParams }
            searchParams.templateFilter = values.templateFilter
            if (!values.templateFilter) {
                delete searchParams.templateFilter
            }
            return ['/workflows', searchParams, router.values.hashParams, { replace: true }]
        },
        setTagFilter: () => {
            const searchParams = { ...router.values.searchParams }
            if (values.tagFilter) {
                searchParams.tagFilter = values.tagFilter
            } else {
                delete searchParams.tagFilter
            }
            return ['/workflows', searchParams, router.values.hashParams, { replace: true }]
        },
    })),
    afterMount(({ actions }) => {
        actions.loadWorkflowTemplates()
    }),
])
