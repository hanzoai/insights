// Mascot mode stories removed

import { Meta } from '@storybook/react'

const meta: Meta = {
    title: 'Components/MascotMode [removed]',
    tags: ['test-skip'],
}
export default meta
export function Placeholder(): JSX.Element {
    return <div>Mascot mode has been removed.</div>
}
