// Mascot stories removed

import { Meta } from '@storybook/react'

const meta: Meta = {
    title: 'Lemon UI/Mascot illustrations [removed]',
    tags: ['test-skip'],
}
export default meta
export function Library(): JSX.Element {
    return <div>Mascot illustrations have been removed.</div>
}
