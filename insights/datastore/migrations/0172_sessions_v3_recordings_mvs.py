# Applied on the live datastore under this name. The v3 recordings materialized views it
# created were retired with the one sessions model, so a fresh bootstrap creates nothing.
operations = []  # type: ignore  # noqa
