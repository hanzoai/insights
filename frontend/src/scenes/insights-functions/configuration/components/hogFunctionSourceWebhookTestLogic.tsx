import { MakeLogicType, actions, connect, kea, key, path, props, reducers, selectors } from 'kea'
import { forms } from 'kea-forms'
import type { DeepPartial, DeepPartialMap, FieldName, ValidationErrorType } from 'kea-forms'

import { publicWebhooksHostOrigin } from 'lib/utils/apiHost'
import { tryJsonParse } from 'lib/utils/json'

import type { InsightsFunctionConfigurationType } from '../../../../types'
import { InsightsFunctionConfigurationLogicProps, insightsFunctionConfigurationLogic } from '../insightsFunctionConfigurationLogic'

export type InsightsFunctionSourceWebhookTestForm = {
    method: string
    headers: string
    query: string
    body: string
    mock_request: boolean
}

export type InsightsFunctionSourceWebhookTestResult = {
    status: number
    body: string
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface insightsFunctionSourceWebhookTestLogicValues {
    configuration: InsightsFunctionConfigurationType // insightsFunctionConfigurationLogic
    templateId: string | undefined // insightsFunctionConfigurationLogic
    exampleCurlRequest: string
    expanded: boolean
    isTestInvocationSubmitting: boolean
    isTestInvocationValid: boolean
    showTestInvocationErrors: boolean
    testInvocation: InsightsFunctionSourceWebhookTestForm
    testInvocationAllErrors: Record<string, any>
    testInvocationChanged: boolean
    testInvocationErrors: DeepPartialMap<InsightsFunctionSourceWebhookTestForm, ValidationErrorType>
    testInvocationHasErrors: boolean
    testInvocationManualErrors: Record<string, any>
    testInvocationTouched: boolean
    testInvocationTouches: Record<string, boolean>
    testInvocationValidationErrors: DeepPartialMap<InsightsFunctionSourceWebhookTestForm, ValidationErrorType>
    testResult: InsightsFunctionSourceWebhookTestResult | null
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface insightsFunctionSourceWebhookTestLogicActions {
    resetTestInvocation: (values?: InsightsFunctionSourceWebhookTestForm) => {
        values?: InsightsFunctionSourceWebhookTestForm
    }
    setTestInvocationManualErrors: (errors: Record<string, any>) => {
        errors: Record<string, any>
    }
    setTestInvocationValue: (
        key: FieldName,
        value: any
    ) => {
        name: FieldName
        value: any
    }
    setTestInvocationValues: (values: DeepPartial<InsightsFunctionSourceWebhookTestForm>) => {
        values: DeepPartial<InsightsFunctionSourceWebhookTestForm>
    }
    setTestResult: (result: InsightsFunctionSourceWebhookTestResult | null) => {
        result: InsightsFunctionSourceWebhookTestResult | null
    }
    submitTestInvocation: () => {
        value: boolean
    }
    submitTestInvocationFailure: (
        error: Error,
        errors: Record<string, any>
    ) => {
        error: Error
        errors: Record<string, any>
    }
    submitTestInvocationRequest: (testInvocation: InsightsFunctionSourceWebhookTestForm) => {
        testInvocation: InsightsFunctionSourceWebhookTestForm
    }
    submitTestInvocationSuccess: (testInvocation: InsightsFunctionSourceWebhookTestForm) => {
        testInvocation: InsightsFunctionSourceWebhookTestForm
    }
    toggleExpanded: (expanded?: boolean) => {
        expanded: boolean | undefined
    }
    touchTestInvocationField: (key: string) => {
        key: string
    }
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface insightsFunctionSourceWebhookTestLogicMeta {
    key: string
    __keaTypeGenInternalSelectorTypes: {
        exampleCurlRequest: (testInvocation: InsightsFunctionSourceWebhookTestForm, arg: any) => string
    }
}

export type insightsFunctionSourceWebhookTestLogicType = MakeLogicType<
    insightsFunctionSourceWebhookTestLogicValues,
    insightsFunctionSourceWebhookTestLogicActions,
    InsightsFunctionConfigurationLogicProps,
    insightsFunctionSourceWebhookTestLogicMeta
>

export const insightsFunctionSourceWebhookTestLogic = kea<insightsFunctionSourceWebhookTestLogicType>([
    props({} as InsightsFunctionConfigurationLogicProps),
    key(({ id, templateId }: InsightsFunctionConfigurationLogicProps) => {
        return id ?? templateId ?? 'new'
    }),

    path((id) => ['scenes', 'pipeline', 'hogfunctions', 'insightsFunctionSourceWebhookTestLogic', id]),
    connect((props: InsightsFunctionConfigurationLogicProps) => ({
        values: [insightsFunctionConfigurationLogic(props), ['configuration', 'templateId']],
    })),
    actions({
        setTestResult: (result: InsightsFunctionSourceWebhookTestResult | null) => ({ result }),
        toggleExpanded: (expanded?: boolean) => ({ expanded }),
    }),
    reducers({
        expanded: [
            false as boolean,
            {
                toggleExpanded: (state, { expanded }) => (expanded === undefined ? !state : expanded),
            },
        ],

        testResult: [
            null as InsightsFunctionSourceWebhookTestResult | null,
            {
                setTestResult: (_, { result }) => result,
            },
        ],
    }),

    forms(({ props, actions }) => ({
        testInvocation: {
            defaults: {
                mock_request: true,
                method: 'POST',
                headers: `{
  "Content-Type": "application/json"
}`,
                query: '',
                body: `{
  "event": "my example event",
  "distinct_id": "webhook-test-123"
}`,
            } as InsightsFunctionSourceWebhookTestForm,
            alwaysShowErrors: true,
            errors: ({ headers, body }) => {
                return {
                    headers: !headers ? 'Required' : tryJsonParse(headers) ? undefined : 'Invalid JSON',
                    body: !body ? 'Required' : tryJsonParse(body) ? undefined : 'Invalid JSON',
                }
            },
            submit: async (data) => {
                actions.setTestResult(null)
                const fetchOptions: RequestInit = {
                    method: data.method,
                    headers: tryJsonParse(data.headers),
                    body: data.method == 'GET' ? undefined : data.body,
                    credentials: 'omit',
                }

                const response = await fetch(
                    `${publicWebhooksHostOrigin()}/public/webhooks/${props.id ?? 'unknown'}${data.query ? `?${data.query}` : ''}`,
                    fetchOptions
                )

                actions.setTestResult({
                    status: response.status,
                    body: await response.text(),
                })
            },
        },
    })),

    selectors({
        exampleCurlRequest: [
            (s) => [s.testInvocation, (_, props) => props],
            (testInvocation: InsightsFunctionSourceWebhookTestForm, props) => {
                const headersJson = tryJsonParse(testInvocation.headers)
                const headers = headersJson
                    ? Object.entries(headersJson)
                          .map(([key, value]) => `-H "${key}: ${value}"`)
                          .join(' ')
                    : ''

                return `curl -X ${testInvocation.method} ${headers} \\
  -d '${testInvocation.body}' \\
  ${publicWebhooksHostOrigin()}/public/webhooks/${props.id ?? 'unknown'}${testInvocation.query ? `?${testInvocation.query}` : ''}`
            },
        ],
    }),
])
