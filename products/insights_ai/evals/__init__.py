"""Built-in eval suites for the Insights AI eval harness (`products.insights_ai.eval_harness`).

One directory per domain; suites are discovered by convention (`eval_*` coroutines in `eval_*.py`).
Product-owned suites live in `products/<product>/evals/` and follow the same conventions.
"""
