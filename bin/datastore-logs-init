#!/usr/bin/env bash
# temporary script to init logs datastore schema before we've got migrations setup properly
bin/wait-for-docker

cat bin/datastore-logs.sql | docker-compose exec -T datastore datastore-client
