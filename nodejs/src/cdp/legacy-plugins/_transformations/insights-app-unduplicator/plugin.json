{
    "name": "Unduplicates",
    "url": "https://github.com/paolodamico/insights-plugin-unduplicates",
    "description": "Prevent duplicates in your data when ingesting.",
    "main": "index.ts",
    "insightsVersion": ">=1.25.0",
    "config": [
        {
            "key": "dedupMode",
            "name": "Dedup Mode",
            "type": "choice",
            "required": true,
            "choices": ["Event and Timestamp", "All Properties"]
        }
    ]
}
