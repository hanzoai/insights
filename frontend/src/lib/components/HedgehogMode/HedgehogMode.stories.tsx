// Hedgehog mode stories removed

import { Meta } from '@storybook/react'

const meta: Meta = {
    title: 'Components/HedgehogMode [removed]',
    tags: ['test-skip'],
}
export default meta
export function Placeholder(): JSX.Element {
    return <div>Hedgehog mode has been removed.</div>
}
