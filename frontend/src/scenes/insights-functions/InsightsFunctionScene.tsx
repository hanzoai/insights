import {
    MakeLogicType,
    BindLogic,
    actions,
    connect,
    kea,
    key,
    listeners,
    path,
    props,
    reducers,
    selectors,
    useActions,
    useValues,
} from 'kea'
import { actionToUrl, router, urlToAction } from 'kea-router'

import { Divider } from '@hanzo/elements'

import { ActivityLog } from 'lib/components/ActivityLog/ActivityLog'
import { NotFound } from 'lib/components/NotFound'
import { FEATURE_FLAGS } from 'lib/constants'
import { useFileSystemLogView } from 'lib/hooks/useFileSystemLogView'
import { Button } from 'lib/elements/Button'
import { More } from 'lib/elements/Button/More'
import { Skeleton } from 'lib/elements/Skeleton'
import { Tab, Tabs } from 'lib/elements/Tabs'
import { Tag } from 'lib/elements/Tag/Tag'
import { featureFlagLogic } from 'lib/logic/featureFlagLogic'
import { capitalizeFirstLetter } from 'lib/utils/strings'
import { DataPipelinesNewSceneKind } from 'scenes/data-pipelines/DataPipelinesNewScene'
import { InsightsFunctionConfiguration } from 'scenes/insights-functions/configuration/InsightsFunctionConfiguration'
import {
    InsightsFunctionConfigurationLogicProps,
    insightsFunctionConfigurationLogic,
} from 'scenes/insights-functions/configuration/insightsFunctionConfigurationLogic'
import { Scene, SceneExport } from 'scenes/sceneTypes'
import { urls } from 'scenes/urls'

import { SceneContent } from '~/layout/scenes/components/SceneContent'
import { SceneTitleSection } from '~/layout/scenes/components/SceneTitleSection'
import {
    ActivityScope,
    Breadcrumb,
    CyclotronJobFilterPropertyFilter,
    InsightsFunctionType,
    InsightsFunctionTypeType,
} from '~/types'

import type { InsightsFunctionConfigurationType } from '../../types'
import { InsightsFunctionBackfills } from './backfills/InsightsFunctionBackfills'
import {
    InsightsFunctionConfigurationClearChangesButton,
    InsightsFunctionConfigurationSaveButton,
} from './configuration/components/InsightsFunctionConfigurationButtons'
import { InsightsFunctionIconEditable } from './configuration/InsightsFunctionIcon'
import { humanizeInsightsFunctionType } from './script-function-utils'
import { HogInvocations } from './invocations/HogInvocations'
import { InsightsFunctionMetrics } from './metrics/InsightsFunctionMetrics'
import { InsightsFunctionSkeleton } from './misc/InsightsFunctionSkeleton'
import { InsightsFunctionRuns } from './runs/InsightsFunctionRuns'

const FN_FUNCTION_SCENE_TABS = ['configuration', 'metrics', 'runs', 'invocations', 'backfills', 'history'] as const
export type InsightsFunctionSceneTab = (typeof FN_FUNCTION_SCENE_TABS)[number]

const InsightsFunctionSceneMapping: Partial<
    Record<InsightsFunctionTypeType, { scene: Scene; url: () => string; newKind?: DataPipelinesNewSceneKind }>
> = {
    transformation: { scene: Scene.Transformations, url: urls.transformations, newKind: 'transformation' },
    // Log transformations have no /pipeline/new page; creation happens from the Logs scene
    transformation_log: { scene: Scene.Logs, url: urls.logs },
    destination: { scene: Scene.Destinations, url: urls.destinations, newKind: 'destination' },
    site_destination: { scene: Scene.Destinations, url: urls.destinations, newKind: 'destination' },
    source_webhook: { scene: Scene.Sources, url: urls.sources, newKind: 'source' },
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface insightsFunctionSceneLogicValues {
    configuration: InsightsFunctionConfigurationType // insightsFunctionConfigurationLogic
    loaded: boolean // insightsFunctionConfigurationLogic
    loading: boolean // insightsFunctionConfigurationLogic
    type: InsightsFunctionTypeType // insightsFunctionConfigurationLogic
    alertId: string | undefined
    breadcrumbs: Breadcrumb[]
    currentTab: InsightsFunctionSceneTab
    isSurveyNotification: boolean
    logicProps: any
    returnTo: string | undefined
    supportsBackfills: boolean
    surveyId: string | undefined
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface insightsFunctionSceneLogicActions {
    loadInsightsFunctionSuccess: (
        insightsFunction: InsightsFunctionType | null,
        payload?: any
    ) => {
        insightsFunction: InsightsFunctionType | null
        payload?: any
    } // insightsFunctionConfigurationLogic
    setCurrentTab: (tab: InsightsFunctionSceneTab) => {
        tab: 'backfills' | 'configuration' | 'history' | 'invocations' | 'metrics' | 'runs'
    }
}

// Generated by kea-typegen. Update if you're an agent, ignore if you're human.
export interface insightsFunctionSceneLogicMeta {
    key: string
    __keaTypeGenInternalSelectorTypes: {
        logicProps: (arg: any) => any
        supportsBackfills: (type: InsightsFunctionTypeType, configuration: InsightsFunctionConfigurationType) => boolean
        alertId: (configuration: InsightsFunctionConfigurationType) => string | undefined
        surveyId: (configuration: InsightsFunctionConfigurationType) => string | undefined
        isSurveyNotification: (configuration: InsightsFunctionConfigurationType) => boolean
        returnTo: (searchParams: Record<string, any>) => string | undefined
        breadcrumbs: (
            type: InsightsFunctionTypeType,
            loading: boolean,
            configuration: InsightsFunctionConfigurationType,
            alertId: string | undefined,
            surveyId: string | undefined,
            isSurveyNotification: boolean,
            returnTo: string | undefined,
            arg: any
        ) => Breadcrumb[]
    }
}

export type insightsFunctionSceneLogicType = MakeLogicType<
    insightsFunctionSceneLogicValues,
    insightsFunctionSceneLogicActions,
    InsightsFunctionConfigurationLogicProps,
    insightsFunctionSceneLogicMeta
>

export const insightsFunctionSceneLogic = kea<insightsFunctionSceneLogicType>([
    props({} as InsightsFunctionConfigurationLogicProps),
    key(({ id, templateId }: InsightsFunctionConfigurationLogicProps) => id ?? templateId ?? 'new'),
    path((key) => ['scenes', 'insights-functions', 'insightsFunctionSceneLogic', key]),
    connect((props: InsightsFunctionConfigurationLogicProps) => ({
        values: [insightsFunctionConfigurationLogic(props), ['configuration', 'type', 'loading', 'loaded']],
        actions: [insightsFunctionConfigurationLogic(props), ['loadInsightsFunctionSuccess']],
    })),
    actions({
        setCurrentTab: (tab: InsightsFunctionSceneTab) => ({ tab }),
    }),
    reducers(() => ({
        currentTab: [
            'configuration' as InsightsFunctionSceneTab,
            {
                setCurrentTab: (_, { tab }) => tab,
            },
        ],
    })),
    selectors({
        logicProps: [() => [(_, props) => props], (props) => props],
        supportsBackfills: [
            (s) => [s.type, s.configuration],
            (type: InsightsFunctionTypeType, configuration: InsightsFunctionType | null): boolean => {
                if (type !== 'destination') {
                    return false
                }
                const source = configuration?.filters?.source ?? 'events'
                return source === 'events'
            },
        ],
        alertId: [
            (s) => [s.configuration],
            (configuration: InsightsFunctionType | null): string | undefined => {
                if (!configuration?.filters?.properties) {
                    return undefined
                }
                const alertIdProp = configuration.filters.properties.find(
                    (p: CyclotronJobFilterPropertyFilter) => p.key === 'alert_id'
                )
                const value = alertIdProp?.value
                return value ? String(value) : undefined
            },
        ],
        surveyId: [
            (s) => [s.configuration],
            (configuration: InsightsFunctionType | null): string | undefined => {
                for (const event of configuration?.filters?.events ?? []) {
                    const surveyIdProp = event.properties?.find((p) => p.key === '$survey_id')
                    if (surveyIdProp?.value) {
                        return String(surveyIdProp.value)
                    }
                }
                return undefined
            },
        ],
        isSurveyNotification: [
            (s) => [s.configuration],
            (configuration: InsightsFunctionType | null): boolean => {
                return (configuration?.filters?.events ?? []).some((e) => e.id === 'survey sent')
            },
        ],
        returnTo: [
            () => [router.selectors.searchParams],
            (searchParams: Record<string, string>): string | undefined => searchParams?.returnTo,
        ],
        breadcrumbs: [
            (s) => [
                s.type,
                s.loading,
                s.configuration,
                s.alertId,
                s.surveyId,
                s.isSurveyNotification,
                s.returnTo,
                (_, props) => props.id ?? null,
            ],
            (
                type: InsightsFunctionTypeType,
                loading: boolean,
                configuration: InsightsFunctionType | null,
                alertId: string | undefined,
                surveyId: string | undefined,
                isSurveyNotification: boolean,
                returnTo: string | undefined,
                id: string | null
            ): Breadcrumb[] => {
                if (loading) {
                    return [
                        {
                            key: Scene.InsightsFunction,
                            name: 'Loading...',
                            iconType: 'data_pipeline',
                        },
                    ]
                }

                const finalCrumb: Breadcrumb = {
                    key: Scene.InsightsFunction,
                    name: configuration?.name || '(Untitled)',
                    iconType: 'data_pipeline',
                }

                const firstEventId = configuration?.filters?.events?.[0]?.id
                const isLogsAlert = typeof firstEventId === 'string' && firstEventId.startsWith('$logs_alert_')

                if (type === 'internal_destination' && isLogsAlert && alertId) {
                    return [
                        {
                            key: Scene.Logs,
                            name: 'Logs',
                            path: `${urls.logs()}?activeTab=alerts`,
                            iconType: 'logs',
                        },
                        {
                            key: Scene.LogsAlertDetail,
                            name: 'Alert',
                            path: returnTo ?? urls.logsAlertDetail(alertId, 'notifications'),
                            iconType: 'logs',
                        },
                        finalCrumb,
                    ]
                }

                if (type === 'internal_destination' && alertId) {
                    // returnTo contains the full path back to the alert edit view
                    // Strip the alert_id param for the insight breadcrumb
                    const alertPath = returnTo ?? urls.alert(alertId!)
                    const insightPath = returnTo ? returnTo.split('?')[0] : urls.alerts()

                    return [
                        {
                            key: Scene.Insight,
                            name: 'Insight',
                            path: insightPath,
                            iconType: 'data_pipeline',
                        },
                        {
                            key: 'alert',
                            name: 'Alert',
                            path: alertPath,
                            iconType: 'data_pipeline',
                        },
                        finalCrumb,
                    ]
                }

                if (isSurveyNotification) {
                    const crumbs: Breadcrumb[] = [
                        {
                            key: Scene.Surveys,
                            name: 'surveys',
                            path: urls.surveys(),
                        },
                    ]
                    if (surveyId) {
                        crumbs.push({
                            key: Scene.Survey,
                            name: 'survey',
                            path: urls.survey(surveyId),
                        })
                    }
                    crumbs.push(finalCrumb)
                    return crumbs
                }

                if (type === 'site_app') {
                    return [
                        {
                            key: Scene.WebScripts,
                            name: 'Web scripts',
                            path: urls.webScripts(),
                            iconType: 'data_pipeline',
                        },
                        finalCrumb,
                    ]
                }

                const sceneMapping = InsightsFunctionSceneMapping[type]

                if (sceneMapping) {
                    return [
                        {
                            key: sceneMapping.scene,
                            name: capitalizeFirstLetter(humanizeInsightsFunctionType(type, true)),
                            path:
                                id || !sceneMapping.newKind
                                    ? sceneMapping.url()
                                    : urls.dataPipelinesNew(sceneMapping.newKind),
                            iconType: 'data_pipeline',
                        },
                        finalCrumb,
                    ]
                }

                if (type === 'internal_destination') {
                    // Returns a Scene that is closest to the element based on the configuration.
                    // This is used to help the InsightsFunctionScene render correct breadcrumbs and redirections
                    if (configuration?.filters?.events?.some((e) => e.id.includes('error_tracking'))) {
                        // Error tracking scene
                        return [
                            {
                                key: Scene.ErrorTracking,
                                name: 'Error tracking',
                                path: urls.errorTracking(),
                            },
                            {
                                key: Scene.InsightsFunction,
                                name: 'Alerts',
                                path: urls.errorTrackingConfiguration() + '#selectedSetting=error-tracking-alerting',
                            },
                            finalCrumb,
                        ]
                    }

                    return [
                        {
                            key: Scene.InsightsFunction,
                            name: 'Notifications',
                            path: returnTo,
                        },
                        finalCrumb,
                    ]
                }
                return [
                    {
                        key: Scene.InsightsFunction,
                        name: 'Function',
                    },
                    finalCrumb,
                ]
            },
        ],
    }),
    actionToUrl(({ values }) => ({
        setCurrentTab: () => {
            return [
                router.values.location.pathname,
                {
                    ...router.values.searchParams,
                    tab: values.currentTab,
                },
                router.values.hashParams,
            ]
        },
    })),
    listeners(({ actions, values }) => ({
        // `type` loads after the URL fires, so a deep link to a tab this type never
        // renders (e.g. ?tab=invocations on a log transformation) must be corrected
        // here too, or Tabs shows no active content.
        loadInsightsFunctionSuccess: () => {
            if (values.type === 'transformation_log' && values.currentTab === 'invocations') {
                actions.setCurrentTab('configuration')
            }
        },
    })),
    urlToAction(({ actions, values }) => {
        const reactToTabChange = (_: any, search: Record<string, string>): void => {
            const possibleTab = (search.tab ?? 'configuration') as InsightsFunctionSceneTab

            let tab = FN_FUNCTION_SCENE_TABS.includes(possibleTab) ? possibleTab : 'configuration'
            if (tab === 'invocations' && values.type === 'transformation_log') {
                tab = 'configuration'
            }
            if (tab !== values.currentTab) {
                actions.setCurrentTab(tab)
            }
        }

        return {
            // All possible routes for this scene need to be listed here
            [urls.insightsFunction(':id')]: reactToTabChange,
            [urls.errorTrackingAlert(':id')]: reactToTabChange,
        }
    }),
])

export const scene: SceneExport<InsightsFunctionConfigurationLogicProps> = {
    component: InsightsFunctionScene,
    logic: insightsFunctionSceneLogic,
    paramsToProps: ({ params: { id, templateId }, hashParams }) => {
        return {
            id,
            templateId,
            subTemplateId: hashParams.configuration?.sub_template_id,
        }
    },
}

function InsightsFunctionHeader(): JSX.Element {
    const { configuration, logicProps, loading, isLegacyPlugin } = useValues(insightsFunctionConfigurationLogic)
    const { setConfigurationValue, duplicate, deleteInsightsFunction } = useActions(insightsFunctionConfigurationLogic)

    return (
        <>
            <SceneTitleSection
                name={configuration.name}
                description={configuration.description || ''}
                resourceType={{
                    type: 'data_pipeline',
                    forceIcon: (
                        <InsightsFunctionIconEditable
                            logicKey={logicProps.id ?? 'new'}
                            src={configuration.icon_url}
                            onChange={(val) => setConfigurationValue('icon_url', val)}
                            size="small"
                        />
                    ),
                }}
                isLoading={loading}
                onNameChange={(value) => setConfigurationValue('name', value)}
                onDescriptionChange={(value) => setConfigurationValue('description', value)}
                canEdit
                actions={
                    <>
                        {!logicProps.templateId && (
                            <>
                                <More
                                    size="small"
                                    overlay={
                                        <>
                                            {!isLegacyPlugin && (
                                                <Button fullWidth onClick={() => duplicate()}>
                                                    Duplicate
                                                </Button>
                                            )}
                                            <Divider />
                                            <Button status="danger" fullWidth onClick={() => deleteInsightsFunction()}>
                                                Delete
                                            </Button>
                                        </>
                                    }
                                />
                                <Divider vertical />
                            </>
                        )}
                        <InsightsFunctionConfigurationClearChangesButton />
                        <InsightsFunctionConfigurationSaveButton />
                    </>
                }
            />
        </>
    )
}

export function InsightsFunctionScene(): JSX.Element {
    const { currentTab, loading, loaded, logicProps, type, supportsBackfills } = useValues(insightsFunctionSceneLogic)
    const { setCurrentTab } = useActions(insightsFunctionSceneLogic)
    const { featureFlags } = useValues(featureFlagLogic)

    const { id, templateId, subTemplateId } = logicProps

    useFileSystemLogView({
        type: `insights_function/${type ?? ''}`,
        ref: id ?? null,
        enabled: Boolean(id && type && loaded),
    })

    if (loading && !loaded) {
        return (
            <div className="flex flex-col gap-4">
                <Skeleton className="w-full h-12" />
                <InsightsFunctionSkeleton />
            </div>
        )
    }

    if (id && !loaded) {
        return <NotFound object="Script function" />
    }

    if (!templateId && !id) {
        return <NotFound object="Script function" />
    }

    const tabs: (Tab<InsightsFunctionSceneTab> | null)[] = [
        {
            label: 'Configuration',
            key: 'configuration',
            content: <InsightsFunctionConfiguration id={id} />,
        },

        type === 'site_app' || type === 'site_destination'
            ? null
            : {
                  label: 'Metrics',
                  key: 'metrics',
                  content: <InsightsFunctionMetrics id={id} />,
              },
        // Log transformations aggregate outcomes per Kafka message and never write
        // per-invocation results, so the Invocations tab would always be empty for them.
        type === 'site_app' || type === 'site_destination' || type === 'transformation_log'
            ? null
            : {
                  label: 'Invocations',
                  key: 'invocations',
                  content: <HogInvocations id={id} functionKind="insights_function" insightsFunctionType={type} />,
              },

        supportsBackfills && featureFlags[FEATURE_FLAGS.BACKFILL_WORKFLOWS_DESTINATION]
            ? {
                  label: (
                      <div className="flex flex-row">
                          <div>Backfills</div>
                          <Tag className="ml-2 uppercase" type="primary">
                              New
                          </Tag>
                      </div>
                  ),
                  key: 'backfills',
                  content: <InsightsFunctionBackfills id={id} />,
              }
            : null,

        supportsBackfills && featureFlags[FEATURE_FLAGS.BACKFILL_WORKFLOWS_DESTINATION]
            ? {
                  label: 'Backfill Runs',
                  key: 'runs',
                  content: <InsightsFunctionRuns id={id} />,
              }
            : null,

        {
            label: 'History',
            key: 'history',
            content: <ActivityLog id={id} scope={ActivityScope.FN_FUNCTION} />,
        },
    ]

    return (
        <SceneContent>
            <BindLogic logic={insightsFunctionConfigurationLogic} props={logicProps}>
                <InsightsFunctionHeader />
                {templateId ? (
                    <InsightsFunctionConfiguration templateId={templateId} subTemplateId={subTemplateId} />
                ) : (
                    <Tabs activeKey={currentTab} tabs={tabs} onChange={setCurrentTab} sceneInset={true} />
                )}
            </BindLogic>
        </SceneContent>
    )
}
