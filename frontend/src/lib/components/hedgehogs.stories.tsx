// Hedgehog stories removed

import { Meta } from '@storybook/react'

const meta: Meta = {
    title: 'Lemon UI/Hog illustrations [removed]',
    tags: ['test-skip'],
}
export default meta
export function Library(): JSX.Element {
    return <div>Hedgehog illustrations have been removed.</div>
}
