#!/bin/sh
[ -f "$(dirname "$0")/_/husky.sh" ] && . "$(dirname "$0")/_/husky.sh"

protected_branch='master'
current_branch=$(git branch --show-current)

if [ "$protected_branch" = "$current_branch" ]
then
    echo "Pushing to $protected_branch is a sin! Instead, create a pull request and repent."
    exit 1 # push will not execute
fi

# Catch deterministic CI failures before this push burns a CI matrix.
# Advisories never block; HOGLI_PREFLIGHT_DISABLED=1 makes it a no-op; no insightscli → skip.
if command -v insightscli >/dev/null 2>&1
then
    insightscli ci:preflight --strict || {
        echo "pre-push: ci:preflight found failures — run 'insightscli ci:preflight --fix', resolve, and push again."
        exit 1
    }
fi

exit 0
