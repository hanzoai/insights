[project]
name = "hanzo-insights-local"
version = "7.9.0"
description = "Hanzo Insights SDK (local shim — real package installed from git fork)"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hanzo_insights"]
