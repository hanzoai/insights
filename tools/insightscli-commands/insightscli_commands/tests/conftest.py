"""Shared test fixtures for insightscli_commands extension tests.

These tests do not require Django. Run from the repo root with:
    pytest tools/insightscli-commands/insightscli_commands/tests/ -p no:django -o 'addopts=' -o 'DJANGO_SETTINGS_MODULE='
"""
