[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "insightscli"
version = "0.1.0"
description = "Developer CLI framework with YAML-based command definitions"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Insights", email = "engineering@hanzo.ai" }
]
keywords = ["cli", "developer-tools", "devex", "yaml"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Build Tools",
    "Topic :: Utilities",
]
dependencies = [
    "click>=8.0",
    "pyyaml>=6.0",
    "requests>=2.28",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[project.scripts]
insightscli = "insightscli.cli:main"

[project.urls]
Homepage = "https://github.com/PostHog/posthog"
Documentation = "https://github.com/PostHog/posthog/tree/master/tools/hogli"
Repository = "https://github.com/PostHog/posthog"

[tool.hatch.build.targets.wheel]
packages = ["src/insightscli"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-p no:warnings -p no:django"
