{
  "name": "@hanzo/stripe",
  "version": "1.3.0",
  "description": "Insights Stripe app",
  "license": "MIT",
  "scripts": {
    "dev": "stripe apps start --manifest stripe-app.dev.json",
    "upload": "npm install --package-lock-only && stripe apps upload",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@stripe/ui-extension-sdk": "catalog:",
    "stripe": "catalog:"
  },
  "devDependencies": {
    "@stripe/ui-extension-tools": "catalog:",
    "@types/jest": "catalog:",
    "jest": "catalog:"
  }
}
