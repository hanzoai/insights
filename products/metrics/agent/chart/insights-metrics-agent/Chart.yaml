apiVersion: v2
name: insights-metrics-agent
description: Scrapes Prometheus /metrics endpoints in your cluster and forwards them to Insights
type: application
version: 0.2.0
# The image tag installs pin to by default. CD publishes the image under this
# tag, so bump it together with `version` to release a new agent.
appVersion: 0.2.0
home: https://hanzo.ai/docs/metrics
sources:
    - https://github.com/Insights/insights/tree/master/products/metrics/agent
