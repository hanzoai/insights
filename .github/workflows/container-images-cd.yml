# Neutralized — the app image ghcr.io/hanzoai/insights is now built and deployed
# by the native Hanzo pipeline at .hanzo/workflows/deploy.yml:
#   Hanzo Git push -> act_runner (in-cluster) -> BuildKit builds ./Dockerfile ->
#   ghcr.io/hanzoai/insights:<sha> -> kubectl patch app insights-web,insights-worker
#   -> operator reconcile -> hanzocd.
# GitHub Actions is now a mirror; this workflow no longer builds or publishes.
name: Container Images CD
on:
  workflow_dispatch:
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
