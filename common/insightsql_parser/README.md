# InsightsQL Parser

Blazing fast InsightsQL parsing. This package can only work in the context of the Insights Django app, as it imports from `posthog.insightsql`.

You can test changes locally by running `pip install ./common/insightsql_parser`
