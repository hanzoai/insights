rules:
    - id: no-direct-persons-db-orm
      message: >-
          Do not query persons DB models via the Django ORM — all calls must
          route through the personinsights gRPC service. Use the client helpers in
          `insights/models/person/util.py` (e.g. `get_person_by_uuid`,
          `get_persons_by_distinct_ids`) and `insights/models/group_type_mapping.py`
          (`get_group_types_for_project`). When adding new access patterns, follow
          the `_personinsights_routed()` pattern in `insights/models/person/util.py`.
          See `insights/personinsights_client/README.md` for client details
          or reach out to #team-ingestion for more information.
      severity: WARNING
      languages: [python]
      patterns:
          - pattern: $MODEL.objects.$METHOD(...)
          - pattern-not: $MODEL.objects.none()
          - metavariable-regex:
                metavariable: $MODEL
                regex: >-
                    ^(Person|PersonDistinctId|PersonlessDistinctId|PersonOverrideMapping|PersonOverride|PendingPersonOverride|FlatPersonOverride|FeatureFlagHashKeyOverride|CohortPeople|Group|GroupTypeMapping)$
      paths:
          exclude:
              # personinsights client implementation
              - '**/insights/personinsights_client/**'
              # personinsights routing helpers (reference the models internally)
              - '**/insights/models/person/util.py'
              - '**/insights/models/group_type_mapping.py'
              # semgrep test cases
              - '**/.semgrep/**'
              # Migrations
              - '**/migrations/**'
              # Test files
              - '**/test/**'
              - '**/tests/**'
              - '**/*_test.py'
              - '**/test_*.py'
              - '**/conftest*.py'
