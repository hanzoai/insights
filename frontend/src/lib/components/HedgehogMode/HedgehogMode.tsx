// Hedgehog mode removed — stub kept for import compatibility

export const HedgeHogModeRenderer = (): null => null

export const getHedgehogModeAssetsUrl = (): string => ''

export type HedgehogModeProps = {
    enabledOverride?: boolean
}

export function HedgehogMode(_props: HedgehogModeProps): JSX.Element | null {
    return null
}
